server:
    port: 8081

spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/edu?serverTimezone=UTC&useUnicode=ture&characterEncoding=utf8&useSSL=false
        username: root
        password: rudghks231!
mybatis:
    mapper-locations: classpath:mapper/**.xml